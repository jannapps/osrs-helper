#!/usr/bin/env bash

# Use the nix flake for this directory
use flake

# Set up database environment
export DATABASE_URL="sqlite3:./db/development.sqlite3"

# Rails environment
export RAILS_ENV=development
export PORT=3000

# Add local bin to PATH for binstubs
export PATH="./bin:$PATH"

